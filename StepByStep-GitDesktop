1-Ouvrir Git desktop et GitHub Web

2-Sur gitWeb copier le URL du project OU telercharger directament dans l`ordinateur

3-Sur GitDesktop ADD (+) nouveau repertoire et choisir un DOSSIER sur le ordinateur = le dossier c`est une copie du WEB

4-AVANT de chaque modification il faut fait un SYNC(PULL) du code REMOTE (Web) à fin de metre le code à jour avant de lui modifier 

5-Coisir le BRANCH OU CREER une branch  = COPIE du MASTER ... Travailler au dessu du branch

6-MODIFIER le code

7-Cliquer sur COMMIT avec description

8-SYNC (=push) pour chequer le modifications

OBS:

MASTER REMOTE est diferent de MASTER LOCAL
